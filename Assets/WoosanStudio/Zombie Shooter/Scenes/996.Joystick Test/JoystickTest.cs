using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace WoosanStudio.ZombieShooter.Test
{
    public class JoystickTest : MonoBehaviour
    {
        /*
        void Update()
        {
            //조이스틱 활성
            if (Input.GetKeyDown(KeyCode.A))
            {
                UltimateJoystick.DisableJoystick("Test_00");
                Debug.Log("활성");
            }

            //조이스틱 비활성
            if (Input.GetKeyDown(KeyCode.S))
            {
                UltimateJoystick.EnableJoystick("Test_00");
                Debug.Log("비활성");
            }

            float h = UltimateJoystick.GetHorizontalAxis("Test_00");
            float v = UltimateJoystick.GetVerticalAxis("Test_00");

            Debug.Log(" h = " + h + " v = " + v);
        }
        */
    }
}
